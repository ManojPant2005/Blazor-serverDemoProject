@page "/new-Student"

@using CrudDemoBlazorServer.Entity

<h5><b>Add New Student</b></h5>


<EditForm Model="Student" OnValidSubmit="SaveStudent">
    <DataAnnotationsValidator/>
    <ValidationSummary/>
    <div class="form-row">
        <div class="form-group col-md-6">
            <label for="inputEmail4">Student Name</label>
            <InputText type="email" class="form-control" id="inputName" @bind-Value="Student.StudentName" placeholder="Enter Student Name" />
        </div>
        <div class="form-group col-md-6">
            <label for="inputPassword4">Student Age</label>
            <InputNumber type="number" class="form-control" @bind-Value="Student.StudentAge" id="inputAge" placeholder="Enter Student Age" />
        </div>
    </div>
    <div class="form-row">
        <div class="form-group col-md-6">
            <label for="inputEmail4">Student Name</label>
            <InputText type="email" class="form-control" id="inputName" @bind-Value="Student.StudentName" placeholder="Enter Student Name" />
        </div>
        <div class="form-group col-md-6">
            <label for="inputPassword4">Student Age</label>
            <InputNumber type="number" class="form-control" @bind-Value="Student.StudentAge" id="inputAge" placeholder="Enter Student Age" />
        </div>
    </div>
    <div class="form-row">
        <div class="form-group col-md-6">
            <label for="inputEmail4">Student Name</label>
            <InputText type="email" class="form-control" id="inputName" @bind-Value="Student.StudentName" placeholder="Enter Student Name" />
        </div>
        <div class="form-group col-md-6">
            <label for="inputPassword4">Student Age</label>
            <InputNumber type="number" class="form-control" @bind-Value="Student.StudentAge" id="inputAge" placeholder="Enter Student Age" />
        </div>
    </div>
    <div class="mt-3">
        <button type="submit" class="btn btn-success">Save Student</button>
        <button type="reset" class="btn btn-secondary">Reset</button>

    </div>

</EditForm>

@code {
    private Student Student = new();

    private void SaveStudent()
    {
        
    }
}
